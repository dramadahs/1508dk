<section class="teaser-content">
  <img src="/phonedisplay.png" alt="Mobiltelefoner med Forbrugs-recap og Green Pass på skærmene" />
  <article>
    <h2>Nyhed: Se dit forbrugs-recap for 2025</h2>
    <p>
      Hvor ofte handlede du økologisk? Og hvor mange gange benyttede du egentlig dit Dankort i 2025?
      Få indsigter om dit personlige forbrugsmønster og se, om du har optjent nok klimapoint til et
      Green Pass - vores belønning til dig, der har handlet særligt klimabevidst i året som gik.
    </p>
    <a class="cta-grey" href="/login/">Se dit forbrugs-recap</a>

    <button id="open-dialog" class="cta-grey"> Læs om Green Pass </button>

    <dialog id="info-dialog">
      <article class="dialog-content">
        <button type="button" class="x-button" aria-label="Close dialog">
          <span></span>
          <span></span>
        </button>
        <h2>Hvad er et <span>Green Pass?</span></h2>
        <p>Green Pass er din belønning for et klimasmart forbrug.</p><p>
          Det fungerer sådan her: Hver gang du handler i en genbrugsbutik eller betaler for dine
          økologiske varer med Dankort, optjener du point. Når du har optjent mindst 50 point, får
          du et Green Pass, som du kan vise i udvalgte reparationsværksteder over hele landet for at
          få op til 100% rabat, når det f.eks. er tid til at reparere din cykel, dine yndlingsjeans
          eller dit seneste genbrugsfund.
        </p><p>Lidt ligesom cashback, men til bæredygtige valg.</p>

        <button id="close-dialog" class="cta-black">Luk</button>
      </article>
    </dialog>
  </article>
</section>

<style>
  dialog {
    overflow: hidden;
    border: none;
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border-radius: var(--border-radius);
    background-color: var(--white-color);
    border: none;
    max-width: 90vw;
    max-height: 90vh;
  }

  dialog::backdrop {
    background: rgba(0, 0, 0, 0.6);
  }

  .dialog-content {
    position: relative;
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
    text-align: left;
    padding: var(--main-padding);
    overflow-y: auto;
    max-height: 80vh;
  }

  .dialog-content h2,
  .dialog-content p {
    color: var(--black-color);
    padding-bottom: var(--top-padding);
  }

  .dialog-content h2 {
    font-size: 24px;
  }

  h2 span {
    color: var(--secondary-color);
  }

  .dialog-content p {
    font-size: 14px;
  }

  .dialog-content button {
    align-self: center;
  }

  .x-button {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 32px;
    height: 32px;
    background: none;
    border: none;
    cursor: pointer;
  }

  .x-button span {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 20px;
    height: 2px;
    background: var(--black-color);
    transform-origin: center;
  }

  .x-button span:first-child {
    transform: translate(-50%, -50%) rotate(45deg);
  }

  .x-button span:last-child {
    transform: translate(-50%, -50%) rotate(-45deg);
  }

  h2,
  p {
    color: var(--white-color);
    padding-bottom: calc(var(--top-padding) * 2);
  }

  .teaser-content {
    background-color: var(--secondary-color);
    display: flex;
    flex-wrap: wrap;
    padding: var(--main-padding);
    align-items: center;
    justify-content: center;
  }

  img {
    display: block;
    max-width: 480px;
    height: 100%;
    margin-right: var(--main-padding);
  }
  article {
    display: flex;
    flex-direction: column;
    max-width: 480px;
  }

  a,
  button {
    align-self: flex-start;
    display: inline-block;
    width: auto;
  }

  a:nth-of-type(1) {
    margin-bottom: var(--top-padding);
  }

  @media (max-width: 1330px) {
    .teaser-content {
      gap: calc(var(--article-gap) / 2);
      padding: var(--main-padding) calc(var(--main-padding) / 2);
    }

    img {
      order: 1;
      margin-right: 0;
      margin-left: var(--main-padding);
    }
  }

  @media (max-width: 1060px) {
    img {
      margin-left: 0;
    }
  }

  @media (max-width: 600px) {
    dialog {
      width: 95vw;
    }
  }

  @media (max-width: 450px) {
    .dialog-content {
      padding: calc(var(--main-padding) / 2);
    }
    .hero-content {
      position: relative;
    }

    img {
      order: -1;
      max-width: 100%;
    }

    a,
    button {
      align-self: center;
    }
  }
</style>
<script>
  const dialog = document.getElementById("info-dialog");
  const openButton = document.getElementById("open-dialog");
  const closeButton = document.getElementById("close-dialog");
  const closeX = document.querySelector(".x-button");

  openButton.addEventListener("click", () => {
    dialog.showModal();
  });

  closeButton.addEventListener("click", () => {
    dialog.close();
  });

  closeX.addEventListener("click", () => {
    dialog.close();
  });
</script>
