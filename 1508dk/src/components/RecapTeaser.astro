---

---

<section class="teaser-content">
  <img src="/recap-phones.png" alt="Mobiltelefoner med Forbrugs-recap og Green Pass på skærmene" />
  <article>
    <h2>Nyhed: Se dit forbrugs-recap for 2025</h2>
    <p>
      Hvor ofte handlede du økologisk? Og hvor mange benyttede du egentlig dit Dankort i 2025? Få
      indsigter om dit personlige forbrugsmønster og se, om du har optjent nok klimapoint til et
      Green Pass - vores belønning til dig, der har handlet særligt klimabevidst i året som gik.
    </p>
    <a class="cta-grey" href="/">Se dit forbrugs-recap</a>
    <a class="cta-grey" href="/">Læs om Green Pass</a>
  </article>
</section>

<style>
  h2,
  p {
    color: var(--white-color);
    padding-bottom: calc(var(--top-padding) * 2);
  }

  .teaser-content {
    background-color: var(--secondary-color);
    display: flex;
    flex-wrap: wrap;
    padding: var(--main-padding);
    align-items: center;
    justify-content: center;
  }

  img {
    display: block;
    max-width: 480px;
    height: 100%;
    margin-right: var(--main-padding);
  }
  article {
    display: flex;
    flex-direction: column;
    max-width: 480px;
  }

  a {
    align-self: flex-start;
    display: inline-block;
    width: auto;
  }

  a:nth-of-type(1) {
    margin-bottom: var(--top-padding);
  }

  @media (max-width: 1330px) {
    .teaser-content {
      gap: calc(var(--article-gap) / 2);
      padding: var(--main-padding) calc(var(--main-padding) / 2);
    }

    img {
      order: 1;
      margin-right: 0;
      margin-left: var(--main-padding);
    }
  }

  @media (max-width: 1060px) {
    img {
      margin-left: 0;
    }
  }

  @media (max-width: 450px) {
    .hero-content {
      position: relative;
    }

    img {
      order: -1;
      max-width: 100%;
    }

    a {
      align-self: center;
    }
  }
</style>
